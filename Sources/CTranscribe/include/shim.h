#ifndef CTRANSCRIBE_SHIM_H
#define CTRANSCRIBE_SHIM_H

// transcribe.cpp's C API (GigaAM and other non-whisper models). The library
// is a dylib that keeps its own ggml inside; see scripts/build-transcribe-macos.sh.
#include "transcribe.h"

#endif
